#include <bits/stdc++.h>
//#include <iostream>
#include <string>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int casos;
    cin >> casos;
    for (int i = 0; i < casos; ++i){
        string aux;
        cin >> aux >> aux;
        cout << "Hola, " << aux << ".\n";
    }

    return 0;
}